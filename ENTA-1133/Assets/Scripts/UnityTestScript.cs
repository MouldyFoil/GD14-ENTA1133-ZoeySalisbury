using UnityEngine;

public class UnityTestScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("HII");
        Debug.LogWarning("GRRR");
        Debug.LogError("STOP");
    }
}
